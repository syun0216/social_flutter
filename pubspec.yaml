name: socialflutter
description: A new Flutter application.

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter_svg: 0.17.3
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter


# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
   - images/Street-Dance-01.jpg
   - images/logo-cat.svg
   - images/user.svg
   - images/password.svg
   - images/check-outline.svg
   - images/check.svg
   - images/comment-outline.svg
   - images/comment-single.svg
   - images/comment.svg
   - images/cross.svg
   - images/date-from.svg
   - images/date-to.svg
   - images/email.svg
   - images/gmap.png
   - images/home.svg
   - images/icon.png
   - images/image_server_error.png
   - images/info-outline.svg
   - images/info.svg
   - images/like-outline.svg
   - images/info.svg
   - images/like-outline.svg
   - images/like.svg
   - images/no-activity.svg
   - images/past-outline.svg
   - images/past.svg
   - images/people.svg
   - images/people-outline.svg
   - images/reply.svg
   - images/search.svg
   - images/search.svg
   - images/send.svg
   - images/splash.png
   - images/time.svg
   - toTop.png
   - user.svg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    - family: SourceSansPro-Bold
      fonts:
        - asset: fonts/SourceSansPro-Bold.ttf
    - family: SourceSansPro-Regular
      fonts:
        - asset: fonts/SourceSansPro-Regular.ttf
    - family: SourceSansPro-SemiBold
      fonts:
        - asset: fonts/SourceSansPro-SemiBold.ttf
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
